__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv*/
venv/
build/
dist/
config.json
config.*.json
!config.example.json
*.sqlite
*.jsonl
.env
.workflow-state.json
# Cached benchmark source PDFs: fetched on first use, never committed. Only the derived
# crops + labels under benchmarks/preprints/ are tracked (self-contained for CI).
benchmarks/preprints/.cache/
